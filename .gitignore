# Runtime data — append-only NDJSON event stores, fetched bodies, logs, observations.
/var/
/cmd/*/var/
/data/
/bar.bak/

# Conversation state for emily-agent.
/conversations/

# Real secrets belong here per the file's own header comment, but the file
# was never actually gitignored — fixed 2026-07-17. History checked clean
# (only CHANGE_ME placeholders were ever committed).
/ops/env.production

# Compiled binaries dropped in repo root by `go build ./cmd/...`.
/entity-graph
/secwatch
/prwatch
!/prwatch/
/processor
/dashboard
/feedserver
/broker
/signalapi
/newssite
/emily-agent
/observation-watcher
/guidance-watcher
/earnings-calendar
/jon-agent
/bob-agent
/form4-watcher
/dividend-watcher
/eps-processor
/eps-reconciler
/schd13-watcher
/prwatch-body
/graph-seeder
/market-data-watcher
/projector
/buyback-watcher
/nt-watcher
/bin/

# Local env / editor noise.
.env
.env.*
!.env.example
*.log
.DS_Store

# Claude Code session directory.
.claude/

# Go workspace (local only)
go.work
go.work.sum
build/
